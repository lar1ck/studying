// Definition of an Object
// An object is a fundamental data type in JavaScript that stores key-value pairs.
// It groups multiple values together — both data (state) and functions (behavior).

// Example:
const dog = {
    name: 'Max',
    age: 5
  };
  
  console.log(dog);
  